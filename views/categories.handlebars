{{!-- Picks every post from this category and renders to page --}}
<div class="container">
    {{#each post}}
    <div class="card">
        <div class="card-header">
            Title: {{header}}
        </div>
        <div class="body">
            {{corntent}}
        </div>
    </div>
    {{/each}}
</div>