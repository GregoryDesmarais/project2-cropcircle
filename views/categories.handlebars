{{!-- Picks every post from this category and renders to page --}}
<!--
<div class="container">
    {{#each post}}
    <div class="card">
        <div class="card-header">
            <a href="/{{category}}/{{id}}"> Title: {{header}}</a>
        </div>
        <div class="body">
            {{corntent}}
        </div>
    </div>
    {{/each}}
</div> -->
<button class="btn btn-primary" id="newPostBtn" data-category="{{category}}">Submit New Post</button>

<div class="accordion" id="accordionExample">
    {{#each post}}
    <div class="card postHeaders">
        <div class="card-header " id="heading{{id}}">
            <h2 class="mb-0">
                <button class="btn btn-link col-12 text-left postTitle" type="button" data-toggle="collapse"
                    data-target="#collapse{{id}}" aria-expanded="true" aria-controls="collapse{{id}}">
                    {{header}}
                </button>
                <br>
                <div class="commentLinkFromPost text-left col-12">
                    <a href="/{{category}}/{{id}}"> Comments</a>
                    <a class="">User Info</a>
                </div>

            </h2>
        </div>

        <div id="collapse{{id}}" class="collapse" aria-labelledby="heading{{id}}" data-parent="#accordionExample">
            <div class="card-body">
                {{corntent}}
            </div>
        </div>
    </div>
    {{/each}}
</div>

<div class="modal" id="newPostModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Create New Post</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <label for="postTitle">Title</label>
                        <input type="text" class="form-control col-12" rows="9" id="newPostTitle"
                            aria-describedby="userName" placeholder="Post Title">
                    </div>
                    <div class="form-group">
                        <label for="newPostContent">Post Body</label>
                        <textarea class="form-control col-12 newPostTextArea" id="newPostContent"
                            placeholder="Post"></textarea>
                    </div>
                    <p class="logInErrorMessage">One or more fields invalid</p>
                </form>
                <button class="btn btn-primary" id="submitNewPostBtn">Submit</button>
            </div>
        </div>
    </div>
</div>